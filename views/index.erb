<!DOCTYPE html>
<html lang="en">
<head>
  <link href="//netdna.bootstrapcdn.com/twitter-bootstrap/2.3.1/css/bootstrap-combined.min.css" rel="stylesheet">
  <script type="text/javascript" src="https://www.google.com/jsapi"></script>
  <script type="text/javascript">
    google.load('visualization', '1', {packages: ['corechart']});
  </script>
  <style type="text/css">
    ul.deals {
      list-style: none;
    }

    ul.deals li {
      margin-top: 0;
    }

    ul.deals li h2 {
      font-size: 20px;
      line-height: 20px;
    }

    ul.deals li h3 {
      font-size: 18px;
      font-weight: normal;
      line-height: 18px;
    }

    ul.deals li h4 {
      font-weight: normal;
    }

    ul.deals li h4 a {
      font-weight: bold;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="row">
      <div class="span12">
        <h1>Deal Forecast</h1>
        <div id="deal-forecast" style="width: 1170px; height: 500px;"></div>
        <h1>Deals</h1>
        <ul class="deals row">


          <% @deals.each do |d| %>
            <li class="media span4">
              <a class="pull-left" href="#">
                <img class="media-object" src="<%= d.parties.first.avatar_url %>">
              </a>
              <div class="media-body">
                <h4 class="media-heading"><a href="<%= @highrise_url %>/deals/<%= d.id %>" title="View <%= d.name %> on Highrise" target="_blank"><%= d.name %></a></h4>
                <h3><%= d.parties.first.name %></h3>
                <h3><%= d.date_range.first.strftime("%m/%Y") %> to <%= d.date_range.last.strftime("%m/%Y") %></h3>
                <h4>$<%= comma_numbers(d.price) %> total | $<%= comma_numbers(d.loe.round) %>/day</h4>
              </div>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
  <script type="text/javascript">
  function drawVisualization() {
    // Create and populate the data table.
    var data = google.visualization.arrayToDataTable(<%= @visualization_array %>);

    // Create and draw the visualization.
    new google.visualization.ColumnChart(document.getElementById('deal-forecast')).
        draw(data,
             {isStacked:true,
              width:1170, height:500,
              hAxis: {title: "Month"},
              vAxis: {format: "$# per day"}}
        );
  }

  google.setOnLoadCallback(drawVisualization);
  </script>
</body>
</html>
